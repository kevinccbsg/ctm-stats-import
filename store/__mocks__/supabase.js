const upsertPlayer = jest.fn();

const upsertEvent = jest.fn();

const upsertMatch = jest.fn();

const loadResult = jest.fn();

module.exports = {
  upsertPlayer,
  upsertEvent,
  upsertMatch,
  loadResult,
};
